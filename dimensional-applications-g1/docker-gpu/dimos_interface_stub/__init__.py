# Stub package — the upstream `dimensional-interface` distribution is missing
# from PyPI for dimos==0.0.12.post2, so this stub satisfies the import so the
# rest of the agentic blueprint can boot. The browser UI is non-functional;
# use `dimos agent-send "..."` instead.
